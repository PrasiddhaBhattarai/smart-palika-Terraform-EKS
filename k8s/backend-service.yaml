apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: sm-app
  labels:
    app: sm-app
    component: backend
spec:
  selector:
    app: sm-app
    component: backend
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000